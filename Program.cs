using Odin;
using System.Linq.Expressions;
using System.Runtime.CompilerServices;
using System.Text;
using static System.Console;
using static System.Math;

namespace Odin
{
    class Program
    {
        static void Main(string[] args)
        {
            //List<string> lista = new List<string>();
            //lista.Add("Binary : Expr left, Token oper, Expr right");
            //lista.Add("Grouping : Expr expression");
            //lista.Add("Literal : Object value");
            //lista.Add("Unary : Token oper, Expr right");
            //defineAST("Expr", lista);

            //Scanner S = new Scanner("string Graveyard while bread =/= ++ -* ^= R%\n");
            //List<Token> lista = new List<Token>();
            //lista = S.ScanTokens();
            //foreach (var token in lista) WriteLine(token);
        }
    }
}